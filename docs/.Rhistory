library(ggplot2)#
library(reshape)#
#
dat <- read.csv("~/Documents/Projects_Current/partitionfinder/docs/downloadstats.csv")#
#
#the version changes in march and may need to be fixed up#
dat <- aggregate(. ~ date, data=dat, FUN=sum)#
#
dat <- dat[order(as.Date(dat$date, format="%d/%m/%Y")),]#
#
dat <- dat[,-c(4)]#
#
dat$Mac <- cumsum(dat$Mac)#
dat$Windows <- cumsum(dat$Windows)#
dat <- melt(dat)#
#
colnames(dat) <- c("date", "OS", "downloads")#
#
dat$date <- as.Date(dat$date, format = "%d/%m/%Y")#
p <- ggplot(dat, aes(date, downloads))#
#
p + geom_area(aes(colour = OS, fill= OS), position = 'stack')
dat
p <- ggplot(dat, aes(date, downloads))#
#
p + geom_area(aes(colour = OS, fill= OS), position = 'stack')
dat
dat <- read.csv("~/Documents/Projects_Current/partitionfinder/docs/downloadstats.csv")#
#
#the version changes in march and may need to be fixed up#
dat <- aggregate(. ~ date, data=dat, FUN=sum)#
#
dat <- dat[order(as.Date(dat$date, format="%d/%m/%Y")),]
dat
dat <- read.csv("~/Documents/Projects_Current/partitionfinder/docs/downloadstats.csv")#
#
#the version changes in march and may need to be fixed up#
dat <- aggregate(. ~ date, data=dat, FUN=sum)#
#
dat <- dat[order(as.Date(dat$date, format="%d/%m/%Y")),]#
#
dat <- dat[,-c(4, 5)]#
#
dat$Mac <- cumsum(dat$Mac)#
dat$Windows <- cumsum(dat$Windows)#
dat <- melt(dat)#
#
colnames(dat) <- c("date", "OS", "downloads")#
#
dat$date <- as.Date(dat$date, format = "%d/%m/%Y")#
p <- ggplot(dat, aes(date, downloads))#
#
p + geom_area(aes(colour = OS, fill= OS), position = 'stack')
the version changes in march and may need to be fixed up#
dat <- aggregate(. ~ date, data=dat, FUN=sum)#
#
dat <- dat[order(as.Date(dat$date, format="%d/%m/%Y")),]#
#
dat <- dat[,-c(4, 5)]#
#
dat$Mac <- cumsum(dat$Mac)#
dat$Windows <- cumsum(dat$Windows)
dat <- read.csv("~/Documents/Projects_Current/partitionfinder/docs/downloadstats.csv")#
#
#the version changes in march and may need to be fixed up#
dat <- aggregate(. ~ date, data=dat, FUN=sum)#
#
dat <- dat[order(as.Date(dat$date, format="%d/%m/%Y")),]#
#
dat <- dat[,-c(4, 5)]
dat
dat <- read.csv("~/Documents/Projects_Current/partitionfinder/docs/downloadstats.csv")
dat
dat <- dat[,-c(4, 5)]
dat
dat <- aggregate(. ~ date, data=dat, FUN=sum)
dat
dat <- dat[order(as.Date(dat$date, format="%d/%m/%Y")),]
dat
dat$Mac <- cumsum(dat$Mac)#
dat$Windows <- cumsum(dat$Windows)
dat
dat <- melt(dat)#
#
colnames(dat) <- c("date", "OS", "downloads")#
#
dat$date <- as.Date(dat$date, format = "%d/%m/%Y")#
p <- ggplot(dat, aes(date, downloads))#
#
p + geom_area(aes(colour = OS, fill= OS), position = 'stack')
library(ggplot2)#
library(reshape)#
#
dat <- read.csv("~/Documents/Projects_Current/partitionfinder/docs/downloadstats.csv")#
#
#the version changes in march and may need to be fixed up#
dat <- dat[,-c(4, 5)]#
#
dat <- aggregate(. ~ date, data=dat, FUN=sum)#
#
dat <- dat[order(as.Date(dat$date, format="%d/%m/%Y")),]#
dat$Mac <- cumsum(dat$Mac)#
dat$Windows <- cumsum(dat$Windows)#
dat <- melt(dat)#
#
colnames(dat) <- c("date", "OS", "downloads")#
#
dat$date <- as.Date(dat$date, format = "%d/%m/%Y")#
p <- ggplot(dat, aes(date, downloads))#
#
p + geom_area(aes(colour = OS, fill= OS), position = 'stack')
dat <- read.csv("~/Documents/Projects_Current/partitionfinder/docs/downloadstats.csv")#
dat.org <- dat
dat.org
dat <- aggregate(. ~ date, data=dat, FUN=sum)
dat
dat.org
plot(dat.org$citations.googlescholar)
?aggregate
dat <- read.csv("~/Documents/Projects_Current/partitionfinder/docs/downloadstats.csv")
dat <- aggregate(. ~ date, data=dat, FUN=sum, na.rm=FALSE)
dat
dat <- aggregate(. ~ date, data=dat, FUN=sum, na.action=na.pass)
dat <- read.csv("~/Documents/Projects_Current/partitionfinder/docs/downloadstats.csv")
dat <- aggregate(. ~ date, data=dat, FUN=sum, na.action=na.pass)
dat
dat <- read.csv("~/Documents/Projects_Current/partitionfinder/docs/downloadstats.csv")
dat
library(ggplot2)#
library(reshape)#
#
dat <- read.csv("~/Documents/Projects_Current/partitionfinder/docs/downloadstats.csv")#
dat.org <- dat#
#
#the version changes in march and may need to be fixed up#
dat <- dat[,-c(4, 5)]#
#
dat <- aggregate(. ~ date, data=dat, FUN=sum)#
#
dat <- dat[order(as.Date(dat$date, format="%d/%m/%Y")),]#
dat$Mac <- cumsum(dat$Mac)#
dat$Windows <- cumsum(dat$Windows)#
dat <- melt(dat)#
#
colnames(dat) <- c("date", "OS", "downloads")#
#
dat$date <- as.Date(dat$date, format = "%d/%m/%Y")#
p <- ggplot(dat, aes(date, downloads))#
#
p + geom_area(aes(colour = OS, fill= OS), position = 'stack')
dat <- read.csv("~/Documents/Projects_Current/partitionfinder/docs/downloadstats.csv")#
dat.org <- dat#
#
#the version changes in march and may need to be fixed up#
dat <- dat[,-c(4, 5)]
dat
dat <- read.csv("~/Documents/Projects_Current/partitionfinder/docs/downloadstats.csv")
dat
library(ggplot2)#
library(reshape)#
#
dat <- read.csv("~/Documents/Projects_Current/partitionfinder/docs/downloadstats.csv")#
dat.org <- dat#
#
#the version changes in march and may need to be fixed up#
dat <- dat[,-c(4, 5, 6)]#
#
dat <- aggregate(. ~ date, data=dat, FUN=sum)#
#
dat <- dat[order(as.Date(dat$date, format="%d/%m/%Y")),]#
dat$Mac <- cumsum(dat$Mac)#
dat$Windows <- cumsum(dat$Windows)#
dat <- melt(dat)#
#
colnames(dat) <- c("date", "OS", "downloads")#
#
dat$date <- as.Date(dat$date, format = "%d/%m/%Y")#
p <- ggplot(dat, aes(date, downloads))#
#
p + geom_area(aes(colour = OS, fill= OS), position = 'stack')
